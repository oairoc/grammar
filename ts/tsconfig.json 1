{
    // 编译器的选项
    "compilerOptions": {
        "strict": true, // 严格检查的总开关
        "alwaysStrict": true, // 以严格模式解析并为每个源文件生成 "use strict"语句
        "module": "ES2015", // 指定生成哪个模块系统代码（commonjs、ES2015）
        "noImplicitAny": false, // 不禁用 any
        "noImplicitThis": true, // 不允许类型不明的this
        // "noEmit": false, // 生成编译后的文件
        "strictNullChecks": true, // 严格地检查空值
        "noEmitOnError": true, // 当有错误时不生成编译后的文件
        "removeComments": true, // 移除注释
        "target": "ES2015", // 编译的目标是什么版本的（ES3、ES5、ES6、ES2016...ES2020、ESNext）
        "outDir": "./dist", // 输出目录
        // "outFile": "./dist/app.js", // outFile仅支持amd或者system
        "allowJs": true, // 允许编译javascript文件。
        "checkJs": true, // 检查js语法是否符合规范
        "lib": [ // 编译过程中需要引入的库文件的列表
            "es5",
            "es2015",
            "es2016",
            "es2017",
            "es2018",
            "dom"
        ]
    },
    // 指定一个匹配列表（属于自动指定该路径下的所有ts相关文件）
    "include": [
        "./src/**/*"
    ],
    // 指定一个排除列表（include的反向操作）
    // 其实有默认值
    // ["node_modules","bower_components","jspm_packages"]
    // "exclude": [
    //     "./src/config/**/*"
    // ],
    // 定义被继承的配置文件
    // "extends": "",
    // 指定哪些文件使用该配置（属于手动一个个指定文件）
    // "files": [
    //     "demo.ts"
    // ]
}